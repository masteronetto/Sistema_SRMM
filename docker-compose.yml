version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: srmm-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Daniel15
      POSTGRES_DB: srmm
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./sql/001_base_crud_usuarios.sql:/docker-entrypoint-initdb.d/01-create-tables.sql
      - ./sql/vistas_reportes.sql:/docker-entrypoint-initdb.d/02-create-views.sql
    networks:
      - srmm-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

networks:
  srmm-network:
    driver: bridge

volumes:
  postgres_data:
